{% extends 'layout.html.twig' %}

{% block title %}Список категорий{% endblock %}

{% block body %}
  <h1>Список категорий</h1>
  {% for category in categories %}
    <a href="{{ path('category_items',{ id:category.id }) }}">{{ category.name }}</a><br>
  {% endfor %}
{% endblock %}